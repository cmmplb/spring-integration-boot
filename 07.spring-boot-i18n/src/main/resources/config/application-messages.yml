# 国际化语言配置
messages:
  # 是否使用消息配置,默认false
  enabled: true
  # 消息包的编码，默认：UTF-8
  encoding: utf_8
  # 消息文件路径名称
  basename: i18n/messages,i18n/validations
  # 加载资源文件的时间，-1：永不过期，默认：-1
  cache-seconds: -1
  # 系统默认语言
  default-language: simplified_chinese
